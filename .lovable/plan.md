

# Revised Plan Updates

Two changes to the approved plan:

## 1. Compact Node Interaction Model
- **Primary interaction (all devices):** Click/tap to expand inline — shows description, tech stack, and optional links directly below the node.
- **Secondary (desktop only):** Hover still highlights the node (subtle glow/scale), but no tooltip. No hover-dependent content.
- Remove the tooltip approach entirely.

## 2. Footer
- Social/icon links only (GitHub, LinkedIn, email/resume).
- Drop the "Built with React" line.

Everything else from the previous plan stays as-is. Ready to build on approval.

